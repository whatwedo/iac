---
# Task files run in order. Tags let you run a subset, e.g. `--tags hardening`.
# To add a task file, drop it in this folder and add an import below.

- import_tasks: 1_base.yml
  tags: [base]

- import_tasks: 2_hardening.yml
  tags: [hardening]
