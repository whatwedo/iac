---
galaxy_info:
  author: whatwedo GmbH
  description: Installs the OpenSSH server and applies baseline SSH hardening.
  license: AGPL-3.0-or-later
  # Keep in step with `requires_ansible` in the collection's meta/runtime.yml.
  min_ansible_version: "2.19"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
  galaxy_tags:
    - ssh
    - sshd
    - openssh
    - hardening
    - security

dependencies: []
